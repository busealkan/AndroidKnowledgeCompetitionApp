<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.h5190059_buse_alkan">

    <application
        android:name=".application.OyunApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_circle"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.OyunSonuActivity" />
        <activity android:name=".activities.HataliCevapActivity" />
        <activity android:name=".activities.OyunActivity" />
        <activity android:name=".activities.KategoriActivity" />
        <activity
            android:name=".activities.OyuncuIsimActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".activities.SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>